# PLC connection (S7-1200 via PUT/GET)
PLC_IP=192.168.0.1
PLC_RACK=0
PLC_SLOT=1
POLL_INTERVAL_MS=500

# PostgreSQL (local dev credentials supplied by the user)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/plc_dashboard

# Dashboard HTTP server
HOST=127.0.0.1
PORT=8000
